BERT is a transformer model widely used for text classification and question answering. BERT uses masked language modeling and fine tuning. BERT is implemented in PyTorch. RoBERTa is a transformer model used for text classification and question answering. RoBERTa uses masked language modeling and fine tuning. GPT is a transformer model used for machine translation and implemented in PyTorch.