Те
перь решим следующую задачу: определим, в какой день 
су
мм
арная выручка по филиалам превысила 500. Код решения пред
ста
влен в листинге 1
1, а результат 
Лист
инг 11 
import
 numpy as np 
import pandas 
data 
= pandas.read_csv('data.csv', index_col='Day')
x
_test= data[['Fil1', 'Fil2', 'Fil3']].as_matrix()
r 
= np.sum(x_test, axis=1) 
print (r) 
print np.nonzero(r>500) 
Код
 нахождения филиала с максимальной средней выручкой 
пр
едставлен в листинге 12, а результат показан на рисунке 
Лист
инг 12 
import
 numpy as np 
import pandas 
data 
= pandas.read_csv('data.csv', index_col='Day')
x
_test= data[['Fil1', 'Fi
l2', 'Fil3']].as_matrix()
r 
= np.mean(x_test, axis=0) 
print (r) 
print np
.nonzero(r== np.max(np.mean(x_test, axis=0)))
180 
 
Рису
нок 51. Решение задачи 
Те
перь решим следующую задачу: определим, в какой день 
м
арная выручка по филиалам превысила 500. Код решения пред
1
1, а результат – на 
рисунке 52. 
Рису
нок 52. Решение задачи 
data 
= pandas.read_csv('data.csv', index_col='Day') 
x
_test= data[['Fil1', 'Fil2', 'Fil3']].as_matrix() 
Код
 нахождения филиала с максимальной средней выручкой 
пр
едставлен в листинге 12, а результат показан на рисунке 
data 
= pandas.read_csv('data.csv', index_col='Day') 
Fi
l2', 'Fil3']].as_matrix() 
print np
.nonzero(r== np.max(np.mean(x_test, axis=0))) 
Те
перь решим следующую задачу: определим, в какой день 
м
арная выручка по филиалам превысила 500. Код решения пред -
 
Код
 нахождения филиала с максимальной средней выручкой 
пр
едставлен в листинге 12, а результат показан на рисунке 53.